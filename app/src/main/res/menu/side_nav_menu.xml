<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Reservation -->
    <item
        android:id="@+id/reservationFragment"
        android:icon="@drawable/reservation"
        android:title="Reservations"
        android:tooltipText="Check reservations"
        app:showAsAction="ifRoom|withText" />
    <!-- Sales Order -->
    <item
        android:id="@+id/salesOrderFragment"
        android:icon="@drawable/order_icon"
        android:title="Sales Orders"
        android:tooltipText="View sales orders"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/menuItemsFragment"
        android:icon="@drawable/menufood"
        android:title="Menu Items"
        android:tooltipText="Manage menu items"
        app:showAsAction="ifRoom|withText" />

    <!-- Account -->
    <item
        android:id="@+id/accountFragment"
        android:icon="@drawable/account"
        android:title="My Account"
        android:tooltipText="Manage account settings"
        app:showAsAction="ifRoom|withText" />

</menu>
